import type { WeekdayHourHeatmapResult } from "@/lib/analytics/types";

const weekdayLabels = ["月", "火", "水", "木", "金", "土", "日"];
function intensityClass(count: number, maxCount: number): string { if (maxCount === 0 || count === 0) return "bg-slate-100 text-slate-700 dark:bg-slate-800 dark:text-slate-200"; const ratio = count / maxCount; if (ratio <= 0.25) return "bg-indigo-100 text-indigo-950 dark:bg-indigo-950 dark:text-indigo-100"; if (ratio <= 0.5) return "bg-indigo-300 text-indigo-950 dark:bg-indigo-800 dark:text-indigo-50"; if (ratio <= 0.75) return "bg-indigo-500 text-white dark:bg-indigo-600"; return "bg-indigo-700 text-white dark:bg-indigo-500"; }

export default function WeekdayHourHeatmap({ heatmap, isPartial }: { heatmap: WeekdayHourHeatmapResult; isPartial: boolean }) {
  return <section className="mt-8 rounded-2xl border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-700 dark:bg-slate-900 sm:p-7" aria-labelledby="heatmap-heading"><div className="flex flex-wrap items-baseline justify-between gap-3"><div><h2 id="heatmap-heading" className="text-2xl font-bold">曜日×時間帯</h2><p className="mt-1 text-sm text-slate-600 dark:text-slate-300">自分の発言数・日本時間（{heatmap.timeZone}）</p></div><p className="text-sm font-medium text-slate-600 dark:text-slate-300">最大値 {heatmap.maxCount}件</p></div>{isPartial && <p className="mt-4 rounded-lg bg-amber-100 px-3 py-2 text-sm font-medium text-amber-950 dark:bg-amber-950 dark:text-amber-50">途中結果です。全データの解析結果ではありません。</p>}<div className="mt-4 overflow-x-auto rounded-xl border border-slate-200 dark:border-slate-700"><table className="min-w-[1100px] border-separate border-spacing-1 text-center text-sm" aria-label="曜日と時間帯ごとの自分の発言数"><thead><tr><th scope="col" className="sticky left-0 z-10 bg-white px-2 py-2 text-left dark:bg-slate-900">曜日</th>{Array.from({ length: 24 }, (_, hour) => <th key={hour} scope="col" className="min-w-9 px-1 py-2 font-medium text-slate-600 dark:text-slate-300">{hour}</th>)}</tr></thead><tbody>{weekdayLabels.map((label, weekday) => <tr key={label}><th scope="row" className="sticky left-0 z-10 bg-white px-2 py-2 text-left font-semibold dark:bg-slate-900">{label}</th>{heatmap.cells.slice(weekday * 24, weekday * 24 + 24).map((cell) => <td key={cell.hour} className="p-0"><span tabIndex={0} aria-label={`${label}曜日 ${cell.hour}時台 ${cell.count}件`} className={`flex min-h-10 min-w-9 items-center justify-center rounded-md font-semibold outline-offset-2 focus:outline focus:outline-2 focus:outline-indigo-700 ${intensityClass(cell.count, heatmap.maxCount)}`}>{cell.count}</span></td>)}</tr>)}</tbody></table></div><div className="mt-4 flex flex-wrap gap-3 text-sm text-slate-600 dark:text-slate-300"><span>凡例:</span>{["0件", "少ない", "やや少ない", "やや多い", "多い"].map((label, index) => <span key={label} className="inline-flex items-center gap-1"><i aria-hidden className={`h-3 w-3 rounded-sm ${intensityClass(index === 0 ? 0 : index, 4)}`} />{label}</span>)}</div><dl className="mt-4 grid gap-2 text-sm sm:grid-cols-2"><div className="rounded-lg bg-slate-100 px-3 py-2 dark:bg-slate-800"><dt className="text-slate-500 dark:text-slate-400">集計対象メッセージ数</dt><dd className="mt-1 text-lg font-bold">{heatmap.includedMessageCount}件</dd></div><div className="rounded-lg bg-slate-100 px-3 py-2 dark:bg-slate-800"><dt className="text-slate-500 dark:text-slate-400">日時不明で除外</dt><dd className="mt-1 text-lg font-bold">{heatmap.excludedMessageCount}件</dd></div></dl><p className="mt-3 text-sm text-slate-500 dark:text-slate-400">スマートフォンでは表を横にスクロールして確認できます。</p></section>;
}
